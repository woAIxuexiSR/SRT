#pragma once

#include <filesystem>

#include "definition.h"
#include "scene.h"
#include "renderer.h"


// class PBRTParser
// {
// private:

// public:
//     PBRTParser() {}

//     void parse(const string& filename, RenderParams renderParams);
//     string next_quoted(std::ifstream& file);
//     string next_bracketed(std::ifstream& file);
//     unordered_map<string, string> next_parameter_list(std::ifstream& file);
// };